1) Basic database design — 3-section SQLite database using Python and SQL
2) Input/entry parameter handling — structured natural-language input extraction
3) API integration and testing — external LLM API calls and response testing
4) Multi-model architecture — separated API adapters with a swappable omni-parser
5) Environment-based secret management — API keys moved from source code into environment variables
6) Currency conversion + fault tolerance — API-based INR→USD conversion with exception handling for network/API failures
7) Database CRUD + input validation — edit/update/delete operations, cancellation paths, and invalid/null input handling
8) Automatic model fallback — provider switching to reduce dependency on a single LLM
9) Extended CRUD + destructive-action confirmation — CRUD for additional lists and confirmation-gated “clear all” functionality
10) Configuration-driven category system — user-defined categories, JSON schema configuration, and dynamic SQLite table creation

RESUME at category-config.py

Additions:
MCP Server


Sites (for help, trust me u need this cause some sections are annoying):
https://aistudio.google.com/docs/api-key
https://mermaid-live-editor.com/
https://aistudio.google.com/


the actual architecture sounds more like:

A modular data-management application with persistent storage, external API integration, provider abstraction, adapter-based parsing, secret management, CRUD operations, input validation, exception handling, and graceful state recovery.