1] Basic database design (with 3 types/sections) using python and sql lite
2] entry/input parameters code
3] api calls and testing
4] separation + setup of multiple api models + swappable omni parser to toggle models
5] saving api key in environment holder instead of directly in code
6] api to convert currency into usd + try catch block to prevent code crash when internet is unavailable during conversion
7] ability to edit, update and delete data from data base + cancelling when you enter a mode accidentally (eg. starting delete but having nothing to delete in list can be escaped without crashing program) + invalid/null as outputs when wrong data is entered 
8] Auto swapping ability for models to prevent lock in with one model during inputs



Additions:
MCP Server
proper modular Python structure
SQLite schema/design
API abstraction
multiple interchangeable model implementations
centralized parser/interface
environment-based secrets
exception handling
API testing
input validation
clean separation of concerns

Sites (for help, trust me u need this cause some sections are annoying):
https://aistudio.google.com/docs/api-key


the actual architecture sounds more like:

A modular data-management application with persistent storage, external API integration, provider abstraction, adapter-based parsing, secret management, CRUD operations, input validation, exception handling, and graceful state recovery.